{
  "name": "independent-ai-safety-network",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "build:data": "node scripts/build-dataset.mjs",
    "build": "npm run validate && npm run build:data && vite build",
    "dev": "npm run build:data && vite",
    "test": "node --test",
    "check": "npm run validate && npm test && npm run build"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "cytoscape": "3.33.4",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
